# Git Workflow Rules

- **NEVER push to GitHub (`git push`) without explicit user permission.**
- Always stage and commit locally when requested or needed, but wait for the user to explicitly ask or approve pushing to the remote repository.
